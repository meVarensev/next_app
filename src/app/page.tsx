'use client';

import { Container } from '@mantine/core';
import { Form } from '@/components/form/form';

function Home() {
    return (
        <main>
            <Container size="md">
                <Form />
            </Container>
        </main>
    );
}

export default Home;
